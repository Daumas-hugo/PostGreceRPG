{% extends 'base.html.twig' %}

{% block title %}Statistique index{% endblock %}

{% block body %}
    <h1>Statistique index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Operator</th>
                <th>Value</th>
                <th>Type</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for statistique in statistiques %}
            <tr>
                <td>{{ statistique.id }}</td>
                <td>{{ statistique.operator }}</td>
                <td>{{ statistique.value }}</td>
                <td>{{ statistique.type }}</td>
                <td>
                    <a href="{{ path('statistique_show', {'id': statistique.id}) }}">show</a>
                    <a href="{{ path('statistique_edit', {'id': statistique.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('statistique_new') }}">Create new</a>
{% endblock %}
